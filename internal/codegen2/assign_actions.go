package codegen2

/*
import "github.com/dcaiafa/lox/internal/util/array"

func (c *context) AssignActions() bool {

}

func (c *context) catalogActionMethods() bool {
	c.ActionMethods = make(map[string]*array.Array[*actionMethod])
	for i := 0; i < c.ParserType.NumMethods(); i++ {
		method := c.ParserType.Method(i)

	}
}
*/
